# Vex Website Update

Separate editable website project. The original `Vex-website` and Windows
builds are unchanged. See `ROADMAP.md` for the supplied launcher requirements.

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
```
